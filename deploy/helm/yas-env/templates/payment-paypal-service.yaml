apiVersion: v1
kind: Service
metadata:
  name: payment-paypal-service
spec:
  type: NodePort
  selector:
    app: payment-paypal-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30017
