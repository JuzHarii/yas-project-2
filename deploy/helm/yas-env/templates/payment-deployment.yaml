apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: "{{ .Values.payment_service.image }}:{{ default .Values.global.defaultTag .Values.payment_service.tag }}"
          ports:
            - containerPort: 8080
