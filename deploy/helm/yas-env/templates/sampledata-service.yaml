apiVersion: v1
kind: Service
metadata:
  name: sampledata-service
spec:
  type: NodePort
  selector:
    app: sampledata-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30016
