apiVersion: v1
kind: Service
metadata:
  name: storefront-bff-service
spec:
  type: NodePort
  selector:
    app: storefront-bff-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30014
